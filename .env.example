# Django settings
SECRET_KEY=your_secure_secret_key_here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com

# Set to True only if you want to force HTTPS (typically in production)
USE_HTTPS=False

# Database settings
# For SQLite (local development):
# Leave DATABASE_URL unset or empty

# For PostgreSQL (production or local PostgreSQL):
# DATABASE_URL=postgres://user:password@localhost:5432/habitsync_db
